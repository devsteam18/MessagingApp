<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.messagingapp.activity.MainActivity"
        android:label="MainActivity" />
    <activity
        android:id="@+id/chatActivity"
        android:name="com.example.messagingapp.activity.ChatActivity"
        android:label="ChatActivity" />
    <activity
        android:id="@+id/loginActivity"
        android:name="com.example.messagingapp.activity.LoginActivity"
        android:label="LoginActivity" />
    <activity
        android:id="@+id/usersActivity"
        android:name="com.example.messagingapp.activity.UsersActivity"
        android:label="UsersActivity" />
    <activity
        android:id="@+id/profileActivity"
        android:name="com.example.messagingapp.activity.ProfileActivity"
        android:label="ProfileActivity" />
</navigation>